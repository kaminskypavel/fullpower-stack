{
  "name": "@fullpower-stack/database",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "dev": "tsnd --rs --debug --poll  src/index.ts",
    "lint": "eslint .",
    "prisma:generate": "prisma generate",
    "prisma:studio": "prisma studio",
    "build": "tsup src/index.ts --format cjs --dts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fakerjs/faker": "^3.0.0",
    "@prisma/client": "^4.5.0"
  },
  "devDependencies": {
    "@fullpower-stack/eslint-config-custom": "workspace:*",
    "@prisma/client": "^4.5.0",
    "prisma": "^4.5.0"
  }
}